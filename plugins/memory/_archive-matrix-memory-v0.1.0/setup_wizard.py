"""Matrix Memory uses Hermes' generic memory setup flow.

This module exists so future provider-specific setup hooks have a stable home,
but v0.1 relies on `get_config_schema()` + `save_config()` only.
"""
